## Stuff to do

* Crawler
    * fix date format and bool in CSV
    * use same date format in scrapy and spider logs
    * fix warning log for njsparser
    * avoid crawling the same car twice in different searches
    * handle 429, handle timeouts when element is not found 
* Frontend
    * add charts suggested in CHART_REVIEW.md
    * add filter to filter cars by multiple criteria (e.g. price range, year range, mileage range, etc.)
    * add a "favorites" feature to allow users to save cars they are interested in
    * fix issue with format locales in tables
